접근제한자 제한자 반환타입 메서드명(매개변수list){
	명령문들
}

제한자: static(정적메소드): new연사자로 객체가 생성되기 전부터 생성되어져있는놈, 같은시점에 생성된 애들을 호출 할 수 있음
	  abstract: 
	  final: 
	  
System.out.println();: 괄호 안에는 반환타입이 있는 메서드만 올 수 있다. void타입은 올 수 없음

return; 제어만 반납(프로그램의 진행 순서만 바꿔줌) 메서드 실행종료.. 하고  다음 메서드 실행

void타입 메서드에서 static메서드를 호출 할 수 있음